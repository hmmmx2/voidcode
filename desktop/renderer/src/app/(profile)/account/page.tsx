import AccountClient from "@/components/Account/AccountClient";

export const metadata = {
  title: "Account — VoidCode",
  description: "Your optional VoidCode account",
};

export default function AccountPage() {
  return <AccountClient />;
}
