import CreditsClient from "@/components/Account/CreditsClient";

export const metadata = {
  title: "Credits — VoidCode",
  description: "Your VoidCode credit balance and history",
};

export default function CreditsPage() {
  return <CreditsClient />;
}
